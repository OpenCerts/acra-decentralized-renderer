import { Meta, Preview, Props, Description, Story } from "@storybook/addon-docs/blocks";
import { LpProfile } from "./lp.profile";
import { lpProfile } from "../samples";
import { action } from "@storybook/addon-actions";
import { object } from "@storybook/addon-knobs";

<Meta title="MDX|LpProfile" component={LpProfile} />

# LpProfile component

<Description of={LpProfile} />

# Props

<Props of={LpProfile} />

# Usage

<Preview withSource>
  <Story name="basic sample">
    <LpProfile document={object("document", lpProfile)} handleObfuscation={action("handleObfuscation")} />
  </Story>
  <Story name="example with warning">
    <LpProfile
      document={object("document", lpProfile)}
      rawDocument={{ signature: { targetHash: "abc" } }}
      handleObfuscation={action("handleObfuscation")}
    />
  </Story>
</Preview>
