import { Meta, Preview, Props, Description, Story } from "@storybook/addon-docs/blocks";
import { CompanyCertificate } from "./company.certificate";
import { companyCertificate } from "../samples";
import { action } from "@storybook/addon-actions";
import { object } from "@storybook/addon-knobs";

<Meta title="MDX|CompanyCertificate" component={CompanyCertificate} />

# CompanyCertificate component

<Description of={CompanyCertificate} />

# Props

<Props of={CompanyCertificate} />

# Usage

<Preview withSource>
  <Story name="basic sample">
    <CompanyCertificate
      document={object("document", companyCertificate)}
      handleObfuscation={action("handleObfuscation")}
    />
  </Story>
</Preview>
