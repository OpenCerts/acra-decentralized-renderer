import { Meta, Preview, Props, Description, Story } from "@storybook/addon-docs/blocks";
import { Business } from "./business";
import { businessCertificate } from "../samples";
import { action } from "@storybook/addon-actions";
import { object } from "@storybook/addon-knobs";

<Meta title="MDX|Business" component={Business} />

# Business component

<Description of={Business} />

# Props

<Props of={Business} />

# Usage

<Preview withSource>
  <Story name="basic sample">
    <Business document={object("document", businessCertificate)} handleObfuscation={action("handleObfuscation")} />
  </Story>
  <Story name="example with warning">
    <Business
      document={object("document", businessCertificate)}
      rawDocument={{ signature: { targetHash: "abc" } }}
      handleObfuscation={action("handleObfuscation")}
    />
  </Story>
</Preview>
