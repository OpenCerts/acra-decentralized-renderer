import { Meta, Preview, Props, Description, Story } from "@storybook/addon-docs/blocks";
import { BusinessCertificate } from "./business.certificate";
import { businessCertificate } from "../samples";
import { action } from "@storybook/addon-actions";
import { object } from "@storybook/addon-knobs";

<Meta title="MDX|BusinessCertificate" component={BusinessCertificate} />

# BusinessCertificate component

<Description of={BusinessCertificate} />

# Props

<Props of={BusinessCertificate} />

# Usage

<Preview withSource>
  <Story name="basic sample">
    <BusinessCertificate
      document={object("document", businessCertificate)}
      handleObfuscation={action("handleObfuscation")}
    />
  </Story>
</Preview>
