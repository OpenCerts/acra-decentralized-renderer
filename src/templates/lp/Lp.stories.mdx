import { Meta, Preview, Props, Description, Story } from "@storybook/addon-docs/blocks";
import { Lp } from "./lp";
import { lpCertificate } from "../samples";
import { action } from "@storybook/addon-actions";
import { object } from "@storybook/addon-knobs";

<Meta title="MDX|Lp" component={Lp} />

# Lp component

<Description of={Lp} />

# Props

<Props of={Lp} />

# Usage

<Preview withSource>
  <Story name="basic sample">
    <Lp document={object("document", lpCertificate)} handleObfuscation={action("handleObfuscation")} />
  </Story>
  <Story name="example with warning">
    <Lp
      document={object("document", lpCertificate)}
      rawDocument={{ signature: { targetHash: "abc" } }}
      handleObfuscation={action("handleObfuscation")}
    />
  </Story>
</Preview>
