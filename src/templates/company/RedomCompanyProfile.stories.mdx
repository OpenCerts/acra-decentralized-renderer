import { Meta, Preview, Props, Description, Story } from "@storybook/addon-docs/blocks";
import { RedomCompanyProfile } from "./redomCompany.profile";
import { redomCompanyProfile } from "../samples";
import { action } from "@storybook/addon-actions";
import { object } from "@storybook/addon-knobs";

<Meta title="MDX|RedomCompanyProfile" component={RedomCompanyProfile} />

# RedomCompanyProfile component

<Description of={RedomCompanyProfile} />

# Props

<Props of={RedomCompanyProfile} />

# Usage

<Preview withSource>
  <Story name="basic sample">
    <RedomCompanyProfile
      document={object("document", redomCompanyProfile)}
      handleObfuscation={action("handleObfuscation")}
    />
  </Story>
  <Story name="example with warning">
    <RedomCompanyProfile
      document={object("document", redomCompanyProfile)}
      rawDocument={{ signature: { targetHash: "abc" } }}
      handleObfuscation={action("handleObfuscation")}
    />
  </Story>
</Preview>
