import { Meta, Preview, Props, Description, Story } from "@storybook/addon-docs/blocks";
import { CompanyProfile } from "./company.profile";
import { companyProfile } from "../samples";
import { action } from "@storybook/addon-actions";
import { object } from "@storybook/addon-knobs";

<Meta title="MDX|CompanyProfile" component={CompanyProfile} />

# CompanyProfile component

<Description of={CompanyProfile} />

# Props

<Props of={CompanyProfile} />

# Usage

<Preview withSource>
  <Story name="basic sample">
    <CompanyProfile document={object("document", companyProfile)} handleObfuscation={action("handleObfuscation")} />
  </Story>
  <Story name="example with warning">
    <CompanyProfile
      document={object("document", companyProfile)}
      rawDocument={{ signature: { targetHash: "abc" } }}
      handleObfuscation={action("handleObfuscation")}
    />
  </Story>
</Preview>
