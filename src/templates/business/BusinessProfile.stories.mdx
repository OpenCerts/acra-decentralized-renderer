import { Meta, Preview, Props, Description, Story } from "@storybook/addon-docs/blocks";
import { BusinessProfile } from "./business.profile";
import { businessProfile } from "../samples";
import { action } from "@storybook/addon-actions";
import { object } from "@storybook/addon-knobs";

<Meta title="MDX|BusinessProfile" component={BusinessProfile} />

# BusinessProfile component

<Description of={BusinessProfile} />

# Props

<Props of={BusinessProfile} />

# Usage

<Preview withSource>
  <Story name="basic sample">
    <BusinessProfile document={object("document", businessProfile)} handleObfuscation={action("handleObfuscation")} />
  </Story>
  <Story name="example with warning">
    <BusinessProfile
      document={object("document", businessProfile)}
      rawDocument={{ signature: { targetHash: "abc" } }}
      handleObfuscation={action("handleObfuscation")}
    />
  </Story>
</Preview>
