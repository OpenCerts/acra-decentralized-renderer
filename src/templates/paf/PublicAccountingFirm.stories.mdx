import { Meta, Preview, Props, Description, Story } from "@storybook/addon-docs/blocks";
import { PublicAccountingFirm } from "./publicAccountingFirm";
import { publicAccountingFirmCertificate } from "../samples";
import { action } from "@storybook/addon-actions";
import { object } from "@storybook/addon-knobs";

<Meta title="MDX|PublicAccountingFirm" component={PublicAccountingFirm} />

# PublicAccountingFirm component

<Description of={PublicAccountingFirm} />

# Props

<Props of={PublicAccountingFirm} />

# Usage

<Preview withSource>
  <Story name="basic sample">
    <PublicAccountingFirm
      document={object("document", publicAccountingFirmCertificate)}
      handleObfuscation={action("handleObfuscation")}
    />
  </Story>
  <Story name="example with warning">
    <PublicAccountingFirm
      document={object("document", publicAccountingFirmCertificate)}
      rawDocument={{ signature: { targetHash: "abc" } }}
      handleObfuscation={action("handleObfuscation")}
    />
  </Story>
</Preview>
