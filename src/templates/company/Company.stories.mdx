import { Meta, Preview, Props, Description, Story } from "@storybook/addon-docs/blocks";
import { Company } from "./company";
import { companyCertificate } from "../sample";
import { action } from "@storybook/addon-actions";
import { object } from "@storybook/addon-knobs";

<Meta title="MDX|Company" component={Company} />

# Company component

<Description of={Company} />

# Props

<Props of={Company} />

# Usage

<Preview withSource>
  <Story name="basic sample">
    <Company document={object("document", companyCertificate)} handleObfuscation={action("handleObfuscation")} />
  </Story>
  <Story name="example with warning">
    <Company
      document={object("document", companyCertificate)}
      rawDocument={{ signature: { targetHash: "abc" } }}
      handleObfuscation={action("handleObfuscation")}
    />
  </Story>
</Preview>
