import { Meta, Preview, Props, Description, Story } from "@storybook/addon-docs/blocks";
import { LlpProfile } from "./llp.profile";
import { llpProfile } from "../samples";
import { action } from "@storybook/addon-actions";
import { object } from "@storybook/addon-knobs";

<Meta title="MDX|LlpProfile" component={LlpProfile} />

# LlpProfile component

<Description of={LlpProfile} />

# Props

<Props of={LlpProfile} />

# Usage

<Preview withSource>
  <Story name="basic sample">
    <LlpProfile document={object("document", llpProfile)} handleObfuscation={action("handleObfuscation")} />
  </Story>
  <Story name="example with warning">
    <LlpProfile
      document={object("document", llpProfile)}
      rawDocument={{ signature: { targetHash: "abc" } }}
      handleObfuscation={action("handleObfuscation")}
    />
  </Story>
</Preview>
